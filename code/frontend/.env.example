# URL of the deployed backend API (no trailing slash), e.g. https://sb-stocks-api.onrender.com
# Leave unset for local dev — Vite's proxy in vite.config.js forwards /api to localhost:5000.
VITE_API_URL=
